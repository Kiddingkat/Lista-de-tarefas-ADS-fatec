#include <stdio.h>
#include <locale.h>

int main(void)
{
	setlocale(LC_ALL, "Portuguese");
	
	float plano, min, total;
	
	printf("quantos minutos você utilizou o plano esse mês: ");
	scanf("%f", &plano);
	
	total = plano * 50,00;
	
	if (min = 50)
	{
		printf("Total: R$50,00");
		scanf("%f", total);
	}
	
	//Receber um número e verificar se ele esta na faixa de 0 à 9. Se sim, mostre uma mensagem afirmativa, caso contrário mostre uma mensagem indicando que o número não se encontra na faixa. 

		return 0;
}
